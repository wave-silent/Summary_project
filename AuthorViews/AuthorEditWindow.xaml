<Window x:Class="LibraryWPFApp.AuthorEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Автор" 
        Height="400" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="WhiteSmoke">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="{Binding WindowTitle}" 
                   FontSize="16" FontWeight="Bold" 
                   Margin="0,0,0,20" HorizontalAlignment="Center"/>

        <!-- Форма -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Фамилия -->
            <TextBlock Grid.Row="0" Grid.Column="0" 
                       Text="Фамилия*:" 
                       Margin="0,0,10,5" VerticalAlignment="Center"/>
            <TextBox Grid.Row="0" Grid.Column="1" 
                     Text="{Binding Fam, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,0,0,5" Height="28"/>
            <TextBlock Grid.Row="0" Grid.Column="1" 
                       Text="{Binding FamError}" 
                       Foreground="Red" FontSize="11" 
                       Margin="0,30,0,0" Height="Auto"/>

            <!-- Имя -->
            <TextBlock Grid.Row="1" Grid.Column="0" 
                       Text="Имя*:" 
                       Margin="0,15,10,5" VerticalAlignment="Center"/>
            <TextBox Grid.Row="1" Grid.Column="1" 
                     Text="{Binding Imya, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,15,0,5" Height="28"/>
            <TextBlock Grid.Row="1" Grid.Column="1" 
                       Text="{Binding ImyaError}" 
                       Foreground="Red" FontSize="11" 
                       Margin="0,30,0,0" Height="Auto"/>

            <!-- Отчество -->
            <TextBlock Grid.Row="2" Grid.Column="0" 
                       Text="Отчество:" 
                       Margin="0,15,10,5" VerticalAlignment="Center"/>
            <TextBox Grid.Row="2" Grid.Column="1" 
                     Text="{Binding Otch, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,15,0,5" Height="28"/>
            <TextBlock Grid.Row="2" Grid.Column="1" 
                       Text="{Binding OtchError}" 
                       Foreground="Red" FontSize="11" 
                       Margin="0,30,0,0" Height="Auto"/>
        </Grid>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="💾 Сохранить" Width="120" Height="32" 
                    Command="{Binding SaveCommand}" 
                    IsDefault="True" Margin="0,0,15,0"
                    FontWeight="SemiBold"/>
            <Button Content="❌ Отмена" Width="120" Height="32" 
                    Command="{Binding CancelCommand}" 
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
