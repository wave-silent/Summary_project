<UserControl x:Class="LibraryWPFApp.AuthorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Управление авторами" 
                   FontSize="18" FontWeight="Bold" 
                   Margin="0,0,0,10"/>

        <!-- Панель управления -->
        <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="1" 
                CornerRadius="5" Padding="10" Background="#F5F5F5">
            <StackPanel Orientation="Horizontal">
                <Button Content="Добавить" Width="80" Height="30" Margin="0,0,5,0"
                        Command="{Binding AddAuthorCommand}"/>
                <Button Content="Редактировать" Width="100" Height="30" Margin="5,0,5,0"
                        Command="{Binding EditAuthorCommand}"/>
                <Button Content="Удалить" Width="80" Height="30" Margin="5,0,5,0"
                        Command="{Binding DeleteAuthorCommand}"/>

                <Separator Width="20" Background="Transparent"/>

                <!-- Поиск -->
                <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="5,0,5,0"/>
                <TextBox Width="200" Height="25" 
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>
        </Border>

        <!-- Таблица авторов -->
        <DataGrid Grid.Row="2" Margin="0,10,0,0" 
                  AutoGenerateColumns="False" 
                  ItemsSource="{Binding Authors}" 
                  SelectedItem="{Binding SelectedAuthor}"
                  IsReadOnly="True" 
                  SelectionMode="Single">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Width="50" Binding="{Binding Author_id}"/>
                <DataGridTextColumn Header="Фамилия" Width="150" Binding="{Binding Fam}"/>
                <DataGridTextColumn Header="Имя" Width="120" Binding="{Binding Imya}"/>
                <DataGridTextColumn Header="Отчество" Width="150" Binding="{Binding Otch}"/>
                <DataGridTextColumn Header="Полное имя" Width="250" Binding="{Binding FullName}"/>
                <DataGridTextColumn Header="Количество книг" Width="120" 
                                    Binding="{Binding Books.Count}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Статус бар -->
        <StatusBar Grid.Row="3" Margin="0,10,0,0">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Text="{Binding AuthorCount, StringFormat='Авторов: {0}'}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>